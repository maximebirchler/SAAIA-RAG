# Prompt — client: M5.2 Settings + user_id + DPAPI

Objectif :
- Persist serverUrl/apiKey/userId
- Protéger apiKey via DPAPI
- Bouton “Test connection” (`GET /ready`)
- Status bar & erreurs visibles

Met à jour docs status + changelog.
