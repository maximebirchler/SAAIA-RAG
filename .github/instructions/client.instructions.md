---
applyTo: "client/**"
---
# Client WinUI — règles (CDC v2.7)

- UX “ChatGPT-like” progressive, mais stable.
- Modes : UI-only / RAG-only / LLM.
- `user_id` stable + `apiKey` protégée DPAPI.
- Streaming : annulation + affichage erreurs.
