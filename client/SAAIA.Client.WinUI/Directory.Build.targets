<Project>
  <!--
    Intentionally left without ItemGroup overrides.
    UserSettingsDialog XAML exclusion is owned by SAAIA.Client.WinUI.csproj so the build behavior
    stays explicit and local to the WinUI project.
  -->
</Project>
